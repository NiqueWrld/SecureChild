@model SecureChild.Models.Student

@{
    ViewData["Title"] = "Create";
}

<h1 style="color: #000;"></h1> <!-- Title in black -->

<h4 style="color: #f00;">Student</h4> <!-- Subheading in red -->
<hr style="border-color: #ffd700;" /> <!-- Horizontal rule in royal yellow -->
<div class="row" style="background-color: #000; color: #fff; padding: 20px;">
    <!-- Background for the form -->
    <div class="col-md-4">
        <form asp-action="CreateStudent">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label" style="color: #fff;"></label> <!-- Label in white -->
                <input asp-for="Name" class="form-control" style="color: black;" /> <!-- Input text in black -->
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Age" class="control-label" style="color: #fff;"></label> <!-- Label in white -->
                <input asp-for="Age" class="form-control" style="color: black;" /> <!-- Input text in black -->
                <span asp-validation-for="Age" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label" style="color: #fff;">Parent</label> <!-- Label in white -->
                <select asp-for="ParentId" class="form-control" asp-items="ViewBag.ParentName"></select>
            </div>
            <div class="form-group">
                <input type="submit" onclick="this.disabled=true; this.form.submit(); this.value='Creating...';" value="Create" class="btn" style="background-color: #ffd700; color: #000;" /> <!-- Create button in royal yellow -->
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn" style="background-color: #ffd700; color: #000; text-decoration: none; padding: 10px 15px; border-radius: 5px;">Back to List</a> <!-- Back to List styled as a button -->
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}
